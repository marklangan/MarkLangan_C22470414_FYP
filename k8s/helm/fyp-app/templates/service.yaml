apiVersion: v1
kind: Service
metadata:
  name: {{ include "fyp-app.fullname" . }}-service
  labels:
    {{- include "fyp-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "fyp-app.selectorLabels" . | nindent 4 }}
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      